<br>
<%= track.title %>
<%= track.description %>
<%= track.timestamp %>
<%= link_to t('tracks.track.buttons.edit'), edit_track_path(track) %>
<%= link_to t('tracks.track.buttons.remove'), track_path(track), method: :delete %>
